---
---
<div class="card-composite-mockup reveal">
  <div class="mockup-header">
    <div class="dots">
      <span class="dot red"></span>
      <span class="dot yellow"></span>
      <span class="dot green"></span>
    </div>
    <div class="mockup-title">training_run_v4.py</div>
  </div>
  <div class="mockup-body">
    <div class="code-panel">
      <pre><code><span class="keyword">import</span> torch
<span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn

<span class="keyword">class</span> <span class="class-name">PlatoonXModel</span>(nn.Module):
    <span class="keyword">def</span> <span class="function">__init__</span>(self, d_model=<span class="number">1024</span>):
        <span class="built-in">super</span>().__init__()
        self.encoder = nn.TransformerEncoder(
            nn.TransformerEncoderLayer(d_model, nhead=<span class="number">16</span>),
            num_layers=<span class="number">24</span>
        )
        
    <span class="keyword">def</span> <span class="function">forward</span>(self, x):
        <span class="comment"># Apply attention across multimodal inputs</span>
        <span class="keyword">return</span> self.encoder(x)
      </code></pre>
    </div>
    <div class="metrics-panel">
      <div class="metric">
        <span class="metric-label">Parameters</span>
        <span class="metric-value tnum">45.2B</span>
      </div>
      <div class="metric">
        <span class="metric-label">Training Loss</span>
        <span class="metric-value tnum">0.8412</span>
      </div>
      <div class="metric">
        <span class="metric-label">Eval MMLU</span>
        <span class="metric-value tnum">82.4%</span>
      </div>
    </div>
  </div>
</div>

<style>
  .card-composite-mockup {
    background-color: var(--color-brand-dark-900);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-level-2);
    overflow: hidden;
    position: relative;
    border: 1px solid rgba(255,255,255,0.1);
  }
  .card-composite-mockup::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.1);
    border-radius: var(--radius-xl);
    pointer-events: none;
  }
  .mockup-header {
    background-color: rgba(0,0,0,0.2);
    padding: var(--space-md) var(--space-xl);
    display: flex;
    align-items: center;
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .dots {
    display: flex;
    gap: 8px;
  }
  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
  .dot.red { background-color: var(--color-ruby); }
  .dot.yellow { background-color: var(--color-lemon); }
  .dot.green { background-color: var(--color-primary-soft); }
  
  .mockup-title {
    flex-grow: 1;
    text-align: center;
    font: var(--text-micro);
    font-family: var(--font-mono);
    color: var(--color-ink-mute);
  }
  .mockup-body {
    display: grid;
    grid-template-columns: 1fr;
  }
  @media (min-width: 768px) {
    .mockup-body {
      grid-template-columns: 2fr 1fr;
    }
  }
  .code-panel {
    padding: var(--space-xl);
    overflow-x: auto;
  }
  .code-panel pre {
    margin: 0;
    font-family: var(--font-mono);
    font: var(--text-body-tabular);
    color: var(--color-on-primary);
    line-height: 1.6;
  }
  .code-panel .keyword { color: var(--color-primary-soft); }
  .code-panel .class-name { color: var(--color-lemon); }
  .code-panel .function { color: var(--color-magenta); }
  .code-panel .built-in { color: var(--color-primary-bg-subdued-hover); }
  .code-panel .number { color: var(--color-ruby); }
  .code-panel .comment { color: var(--color-ink-mute); }
  
  .metrics-panel {
    background-color: rgba(255,255,255,0.02);
    border-left: 1px solid rgba(255,255,255,0.05);
    padding: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }
  .metric {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }
  .metric-label {
    font: var(--text-micro);
    color: var(--color-ink-mute);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  .metric-value {
    font: var(--text-display-md);
    color: var(--color-on-primary);
  }
</style>
